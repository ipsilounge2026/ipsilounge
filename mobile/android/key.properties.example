# 이 파일을 복사해서 key.properties로 만든 뒤 실제 값 입력
# key.properties는 .gitignore에 포함되어 있어 Git에 올라가지 않습니다.

storePassword=YOUR_KEYSTORE_PASSWORD
keyPassword=YOUR_KEY_PASSWORD
keyAlias=ipsilounge
storeFile=../ipsilounge-release.jks
